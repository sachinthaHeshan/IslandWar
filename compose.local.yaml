# Local host ports. Dokploy ignores this file.
# docker network create dokploy-network
# docker compose -f compose.yaml -f compose.local.yaml up --build
services:
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
  backend:
    ports:
      - "8201:8201"
  frontend:
    ports:
      - "8200:8200"
